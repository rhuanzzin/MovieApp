import { keyframes } from "styled-components";

    const ScaleUp = keyframes`
            0%{
            transform: scale(60%);
        }
        100%{
            transform: scale(100%);
        }
        `

export default ScaleUp;